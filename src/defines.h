#ifndef DEFINES_H
#define DEFINES_H

#define WINDOW_WIDTH 512
#define WINDOW_HEIGHT 512

#define RASTER_WIDTH 512
#define RASTER_HEIGHT 512

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#define SQR(a) ((a) * (a))
#define STEP 1

#endif
