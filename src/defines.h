#ifndef DEFINES_H
#define DEFINES_H

#define WINDOW_WIDTH 1024
#define WINDOW_HEIGHT 1024

#define RASTER_WIDTH 4096
#define RASTER_HEIGHT 4096

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#define SQR(a) ((a) * (a))
#define STEP 1

#endif
