{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug collatz",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "${defaultBuildTask}",
      "program": "${workspaceFolder}/bin/collatz"
    }
  ]
}
